// frontend/hotel-ui/tailwind.config.js
export default {
    // v4 works zero-config, but this keeps JSX/HTML scanning explicit
    content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"],
    theme: { extend: {} },
    plugins: [],
};
